# persistence_map_linux
Карта персистентности Linux
-----------------------------


Автономный инструмент сбора forensic-данных для Linux систем — собирает все ключевые артефакты для расследования инцидентов безопасности за 7 дней с автоматическим созданием запароленного ZIP-архива.

🚀 Особенности
Forensic сбора:
- автоматическая карта персистентности (MITRE ATT&CK);
- история команд всех пользователей с timestamps;
- Email отправка результатов через telnet SMTP;
- поддержка Ubuntu/Debian/RHEL и других дистрибутивов;
- запароленный ZIP архив для безопасной передачи в /tmp;
- подробный отчет с timeline и метаданными.

### 📁 Собранные артефакты
Forensic

├── authorization_logs/     # Логи авторизации, SSH, sudo

├── bash_hist/             # История команд всех пользователей

├── Persistence Linux/     # Механизмы персистентности (cron, systemd, .bashrc)

├── general_logs/          # Syslog, journalctl, auditd

├── network/               # Сетевые соединения, firewall, SSH config

├── packets_installed_info/ # История apt/dpkg/rpm установок

├── SystemInfo/            # Аппаратная + системная информация

└── incident_info.txt      # Метаданные инцидента

⚙️ Настройка Email отправки

Раскомментируйте и заполните в конце скрипта:

SMTP_SERVER="your.smtp.server"

SMTP_PORT="25"

EMAIL_FROM="forensic@yourcompany.com" 

EMAIL_TO="soc@yourcompany.com"

🛡️ Безопасность

Пароль архива: PASSWORD (обязательно измените!)

Логи ошибок: errors.log в архиве

Временные файлы: Автоочистка через rm -rf /tmp/forensic_*

Нет сетевых подключений кроме опциональной SMTP

🛠️Интеграция с Wazuh

- добавьте файл по пути /var/ossec/integrations

- настройте конфигурацию Active-Response в ossec.conf по пути /var/ossec/etc/ossec.conf задав rule.id при котором будет вызываться сбор артефактов
